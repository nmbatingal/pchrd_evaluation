<?php

// Data functions (insert, update, delete, form) for table responses

// This script and data application were generated by AppGini 5.74
// Download AppGini for free from https://bigprof.com/appgini/download/

function responses_insert(){
	global $Translation;

	// mm: can member insert record?
	$arrPerm=getTablePermissions('responses');
	if(!$arrPerm[1]){
		return false;
	}

	$data['respondent_id'] = makeSafe($_REQUEST['respondent_id']);
		if($data['respondent_id'] == empty_lookup_value){ $data['respondent_id'] = ''; }
	$data['question_id'] = makeSafe($_REQUEST['question_id']);
		if($data['question_id'] == empty_lookup_value){ $data['question_id'] = ''; }
	$data['response'] = br2nl(makeSafe($_REQUEST['response']));
	if($data['response']== ''){
		echo StyleSheet() . "\n\n<div class=\"alert alert-danger\">" . $Translation['error:'] . " 'Response': " . $Translation['field not null'] . '<br><br>';
		echo '<a href="" onclick="history.go(-1); return false;">'.$Translation['< back'].'</a></div>';
		exit;
	}

	// hook: responses_before_insert
	if(function_exists('responses_before_insert')){
		$args=array();
		if(!responses_before_insert($data, getMemberInfo(), $args)){ return false; }
	}

	$o = array('silentErrors' => true);
	sql('insert into `responses` set       `respondent_id`=' . (($data['respondent_id'] !== '' && $data['respondent_id'] !== NULL) ? "'{$data['respondent_id']}'" : 'NULL') . ', `question_id`=' . (($data['question_id'] !== '' && $data['question_id'] !== NULL) ? "'{$data['question_id']}'" : 'NULL') . ', `response`=' . (($data['response'] !== '' && $data['response'] !== NULL) ? "'{$data['response']}'" : 'NULL'), $o);
	if($o['error']!=''){
		echo $o['error'];
		echo "<a href=\"responses_view.php?addNew_x=1\">{$Translation['< back']}</a>";
		exit;
	}

	$recID = db_insert_id(db_link());

	// hook: responses_after_insert
	if(function_exists('responses_after_insert')){
		$res = sql("select * from `responses` where `id`='" . makeSafe($recID, false) . "' limit 1", $eo);
		if($row = db_fetch_assoc($res)){
			$data = array_map('makeSafe', $row);
		}
		$data['selectedID'] = makeSafe($recID, false);
		$args=array();
		if(!responses_after_insert($data, getMemberInfo(), $args)){ return $recID; }
	}

	// mm: save ownership data
	set_record_owner('responses', $recID, getLoggedMemberID());

	return $recID;
}

function responses_delete($selected_id, $AllowDeleteOfParents=false, $skipChecks=false){
	// insure referential integrity ...
	global $Translation;
	$selected_id=makeSafe($selected_id);

	// mm: can member delete record?
	$arrPerm=getTablePermissions('responses');
	$ownerGroupID=sqlValue("select groupID from membership_userrecords where tableName='responses' and pkValue='$selected_id'");
	$ownerMemberID=sqlValue("select lcase(memberID) from membership_userrecords where tableName='responses' and pkValue='$selected_id'");
	if(($arrPerm[4]==1 && $ownerMemberID==getLoggedMemberID()) || ($arrPerm[4]==2 && $ownerGroupID==getLoggedGroupID()) || $arrPerm[4]==3){ // allow delete?
		// delete allowed, so continue ...
	}else{
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: responses_before_delete
	if(function_exists('responses_before_delete')){
		$args=array();
		if(!responses_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'];
	}

	sql("delete from `responses` where `id`='$selected_id'", $eo);

	// hook: responses_after_delete
	if(function_exists('responses_after_delete')){
		$args=array();
		responses_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("delete from membership_userrecords where tableName='responses' and pkValue='$selected_id'", $eo);
}

function responses_update($selected_id){
	global $Translation;

	// mm: can member edit record?
	$arrPerm=getTablePermissions('responses');
	$ownerGroupID=sqlValue("select groupID from membership_userrecords where tableName='responses' and pkValue='".makeSafe($selected_id)."'");
	$ownerMemberID=sqlValue("select lcase(memberID) from membership_userrecords where tableName='responses' and pkValue='".makeSafe($selected_id)."'");
	if(($arrPerm[3]==1 && $ownerMemberID==getLoggedMemberID()) || ($arrPerm[3]==2 && $ownerGroupID==getLoggedGroupID()) || $arrPerm[3]==3){ // allow update?
		// update allowed, so continue ...
	}else{
		return false;
	}

	$data['respondent_id'] = makeSafe($_REQUEST['respondent_id']);
		if($data['respondent_id'] == empty_lookup_value){ $data['respondent_id'] = ''; }
	$data['question_id'] = makeSafe($_REQUEST['question_id']);
		if($data['question_id'] == empty_lookup_value){ $data['question_id'] = ''; }
	$data['response'] = br2nl(makeSafe($_REQUEST['response']));
	if($data['response']==''){
		echo StyleSheet() . "\n\n<div class=\"alert alert-danger\">{$Translation['error:']} 'Response': {$Translation['field not null']}<br><br>";
		echo '<a href="" onclick="history.go(-1); return false;">'.$Translation['< back'].'</a></div>';
		exit;
	}
	$data['selectedID']=makeSafe($selected_id);

	// hook: responses_before_update
	if(function_exists('responses_before_update')){
		$args=array();
		if(!responses_before_update($data, getMemberInfo(), $args)){ return false; }
	}

	$o=array('silentErrors' => true);
	sql('update `responses` set       `respondent_id`=' . (($data['respondent_id'] !== '' && $data['respondent_id'] !== NULL) ? "'{$data['respondent_id']}'" : 'NULL') . ', `question_id`=' . (($data['question_id'] !== '' && $data['question_id'] !== NULL) ? "'{$data['question_id']}'" : 'NULL') . ', `response`=' . (($data['response'] !== '' && $data['response'] !== NULL) ? "'{$data['response']}'" : 'NULL') . " where `id`='".makeSafe($selected_id)."'", $o);
	if($o['error']!=''){
		echo $o['error'];
		echo '<a href="responses_view.php?SelectedID='.urlencode($selected_id)."\">{$Translation['< back']}</a>";
		exit;
	}


	// hook: responses_after_update
	if(function_exists('responses_after_update')){
		$res = sql("SELECT * FROM `responses` WHERE `id`='{$data['selectedID']}' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)){
			$data = array_map('makeSafe', $row);
		}
		$data['selectedID'] = $data['id'];
		$args = array();
		if(!responses_after_update($data, getMemberInfo(), $args)){ return; }
	}

	// mm: update ownership data
	sql("update membership_userrecords set dateUpdated='".time()."' where tableName='responses' and pkValue='".makeSafe($selected_id)."'", $eo);

}

function responses_form($selected_id = '', $AllowUpdate = 1, $AllowInsert = 1, $AllowDelete = 1, $ShowCancel = 0, $TemplateDV = '', $TemplateDVP = ''){
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selected_id. If $selected_id
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;

	// mm: get table permissions
	$arrPerm=getTablePermissions('responses');
	if(!$arrPerm[1] && $selected_id==''){ return ''; }
	$AllowInsert = ($arrPerm[1] ? true : false);
	// print preview?
	$dvprint = false;
	if($selected_id && $_REQUEST['dvprint_x'] != ''){
		$dvprint = true;
	}

	$filterer_respondent_id = thisOr(undo_magic_quotes($_REQUEST['filterer_respondent_id']), '');
	$filterer_question_id = thisOr(undo_magic_quotes($_REQUEST['filterer_question_id']), '');

	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: respondent_id
	$combo_respondent_id = new DataCombo;
	// combobox: question_id
	$combo_question_id = new DataCombo;

	if($selected_id){
		// mm: check member permissions
		if(!$arrPerm[2]){
			return "";
		}
		// mm: who is the owner?
		$ownerGroupID=sqlValue("select groupID from membership_userrecords where tableName='responses' and pkValue='".makeSafe($selected_id)."'");
		$ownerMemberID=sqlValue("select lcase(memberID) from membership_userrecords where tableName='responses' and pkValue='".makeSafe($selected_id)."'");
		if($arrPerm[2]==1 && getLoggedMemberID()!=$ownerMemberID){
			return "";
		}
		if($arrPerm[2]==2 && getLoggedGroupID()!=$ownerGroupID){
			return "";
		}

		// can edit?
		if(($arrPerm[3]==1 && $ownerMemberID==getLoggedMemberID()) || ($arrPerm[3]==2 && $ownerGroupID==getLoggedGroupID()) || $arrPerm[3]==3){
			$AllowUpdate=1;
		}else{
			$AllowUpdate=0;
		}

		$res = sql("select * from `responses` where `id`='".makeSafe($selected_id)."'", $eo);
		if(!($row = db_fetch_array($res))){
			return error_message($Translation['No records found'], 'responses_view.php', false);
		}
		$urow = $row; /* unsanitized data */
		$hc = new CI_Input();
		$row = $hc->xss_clean($row); /* sanitize data */
		$combo_respondent_id->SelectedData = $row['respondent_id'];
		$combo_question_id->SelectedData = $row['question_id'];
	}else{
		$combo_respondent_id->SelectedData = $filterer_respondent_id;
		$combo_question_id->SelectedData = $filterer_question_id;
	}
	$combo_respondent_id->HTML = '<span id="respondent_id-container' . $rnd1 . '"></span><input type="hidden" name="respondent_id" id="respondent_id' . $rnd1 . '" value="' . html_attr($combo_respondent_id->SelectedData) . '">';
	$combo_respondent_id->MatchText = '<span id="respondent_id-container-readonly' . $rnd1 . '"></span><input type="hidden" name="respondent_id" id="respondent_id' . $rnd1 . '" value="' . html_attr($combo_respondent_id->SelectedData) . '">';
	$combo_question_id->HTML = '<span id="question_id-container' . $rnd1 . '"></span><input type="hidden" name="question_id" id="question_id' . $rnd1 . '" value="' . html_attr($combo_question_id->SelectedData) . '">';
	$combo_question_id->MatchText = '<span id="question_id-container-readonly' . $rnd1 . '"></span><input type="hidden" name="question_id" id="question_id' . $rnd1 . '" value="' . html_attr($combo_question_id->SelectedData) . '">';

	ob_start();
	?>

	<script>
		// initial lookup values
		AppGini.current_respondent_id__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['respondent_id'] : $filterer_respondent_id); ?>"};
		AppGini.current_question_id__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['question_id'] : $filterer_question_id); ?>"};

		jQuery(function() {
			setTimeout(function(){
				if(typeof(respondent_id_reload__RAND__) == 'function') respondent_id_reload__RAND__();
				if(typeof(question_id_reload__RAND__) == 'function') question_id_reload__RAND__();
			}, 10); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
		function respondent_id_reload__RAND__(){
		<?php if(($AllowUpdate || $AllowInsert) && !$dvprint){ ?>

			$j("#respondent_id-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c){
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_respondent_id__RAND__.value, t: 'responses', f: 'respondent_id' },
						success: function(resp){
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="respondent_id"]').val(resp.results[0].id);
							$j('[id=respondent_id-container-readonly__RAND__]').html('<span id="respondent_id-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=respondents_view_parent]').hide(); }else{ $j('.btn[id=respondents_view_parent]').show(); }


							if(typeof(respondent_id_update_autofills__RAND__) == 'function') respondent_id_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term){ /* */ return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page){ /* */ return { s: term, p: page, t: 'responses', f: 'respondent_id' }; },
					results: function(resp, page){ /* */ return resp; }
				},
				escapeMarkup: function(str){ /* */ return str; }
			}).on('change', function(e){
				AppGini.current_respondent_id__RAND__.value = e.added.id;
				AppGini.current_respondent_id__RAND__.text = e.added.text;
				$j('[name="respondent_id"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=respondents_view_parent]').hide(); }else{ $j('.btn[id=respondents_view_parent]').show(); }


				if(typeof(respondent_id_update_autofills__RAND__) == 'function') respondent_id_update_autofills__RAND__();
			});

			if(!$j("#respondent_id-container__RAND__").length){
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_respondent_id__RAND__.value, t: 'responses', f: 'respondent_id' },
					success: function(resp){
						$j('[name="respondent_id"]').val(resp.results[0].id);
						$j('[id=respondent_id-container-readonly__RAND__]').html('<span id="respondent_id-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=respondents_view_parent]').hide(); }else{ $j('.btn[id=respondents_view_parent]').show(); }

						if(typeof(respondent_id_update_autofills__RAND__) == 'function') respondent_id_update_autofills__RAND__();
					}
				});
			}

		<?php }else{ ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_respondent_id__RAND__.value, t: 'responses', f: 'respondent_id' },
				success: function(resp){
					$j('[id=respondent_id-container__RAND__], [id=respondent_id-container-readonly__RAND__]').html('<span id="respondent_id-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=respondents_view_parent]').hide(); }else{ $j('.btn[id=respondents_view_parent]').show(); }

					if(typeof(respondent_id_update_autofills__RAND__) == 'function') respondent_id_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
		function question_id_reload__RAND__(){
		<?php if(($AllowUpdate || $AllowInsert) && !$dvprint){ ?>

			$j("#question_id-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c){
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_question_id__RAND__.value, t: 'responses', f: 'question_id' },
						success: function(resp){
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="question_id"]').val(resp.results[0].id);
							$j('[id=question_id-container-readonly__RAND__]').html('<span id="question_id-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=session_questions_view_parent]').hide(); }else{ $j('.btn[id=session_questions_view_parent]').show(); }


							if(typeof(question_id_update_autofills__RAND__) == 'function') question_id_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term){ /* */ return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page){ /* */ return { s: term, p: page, t: 'responses', f: 'question_id' }; },
					results: function(resp, page){ /* */ return resp; }
				},
				escapeMarkup: function(str){ /* */ return str; }
			}).on('change', function(e){
				AppGini.current_question_id__RAND__.value = e.added.id;
				AppGini.current_question_id__RAND__.text = e.added.text;
				$j('[name="question_id"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=session_questions_view_parent]').hide(); }else{ $j('.btn[id=session_questions_view_parent]').show(); }


				if(typeof(question_id_update_autofills__RAND__) == 'function') question_id_update_autofills__RAND__();
			});

			if(!$j("#question_id-container__RAND__").length){
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_question_id__RAND__.value, t: 'responses', f: 'question_id' },
					success: function(resp){
						$j('[name="question_id"]').val(resp.results[0].id);
						$j('[id=question_id-container-readonly__RAND__]').html('<span id="question_id-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=session_questions_view_parent]').hide(); }else{ $j('.btn[id=session_questions_view_parent]').show(); }

						if(typeof(question_id_update_autofills__RAND__) == 'function') question_id_update_autofills__RAND__();
					}
				});
			}

		<?php }else{ ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_question_id__RAND__.value, t: 'responses', f: 'question_id' },
				success: function(resp){
					$j('[id=question_id-container__RAND__], [id=question_id-container-readonly__RAND__]').html('<span id="question_id-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>'){ $j('.btn[id=session_questions_view_parent]').hide(); }else{ $j('.btn[id=session_questions_view_parent]').show(); }

					if(typeof(question_id_update_autofills__RAND__) == 'function') question_id_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_contents());
	ob_end_clean();


	// code for template based detail view forms

	// open the detail view template
	if($dvprint){
		$template_file = is_file("./{$TemplateDVP}") ? "./{$TemplateDVP}" : './templates/responses_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	}else{
		$template_file = is_file("./{$TemplateDV}") ? "./{$TemplateDV}" : './templates/responses_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Response details', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', ($_REQUEST['Embedded'] ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($AllowInsert){
		if(!$selected_id) $templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1" onclick="return responses_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1" onclick="return responses_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	}else{
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if($_REQUEST['Embedded']){
		$backAction = 'AppGini.closeParentModal(); return false;';
	}else{
		$backAction = '$j(\'form\').eq(0).attr(\'novalidate\', \'novalidate\'); document.myform.reset(); return true;';
	}

	if($selected_id){
		if(!$_REQUEST['Embedded']) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" onclick="$j(\'form\').eq(0).prop(\'novalidate\', true); document.myform.reset(); return true;" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($AllowUpdate){
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" onclick="return responses_validateData();" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		}else{
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		}
		if(($arrPerm[4]==1 && $ownerMemberID==getLoggedMemberID()) || ($arrPerm[4]==2 && $ownerGroupID==getLoggedGroupID()) || $arrPerm[4]==3){ // allow delete?
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" onclick="return confirm(\'' . $Translation['are you sure?'] . '\');" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		}else{
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		}
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	}else{
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', ($ShowCancel ? '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>' : ''), $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(($selected_id && !$AllowUpdate && !$AllowInsert) || (!$selected_id && !$AllowInsert)){
		$jsReadOnly .= "\tjQuery('#respondent_id').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#respondent_id_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#question_id').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#question_id_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#response').replaceWith('<div class=\"form-control-static\" id=\"response\">' + (jQuery('#response').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('.select2-container').hide();\n";

		$noUploads = true;
	}elseif($AllowInsert){
		$jsEditable .= "\tjQuery('form').eq(0).data('already_changed', true);"; // temporarily disable form change handler
			$jsEditable .= "\tjQuery('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(respondent_id)%%>', $combo_respondent_id->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(respondent_id)%%>', $combo_respondent_id->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(respondent_id)%%>', urlencode($combo_respondent_id->MatchText), $templateCode);
	$templateCode = str_replace('<%%COMBO(question_id)%%>', $combo_question_id->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(question_id)%%>', $combo_question_id->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(question_id)%%>', urlencode($combo_question_id->MatchText), $templateCode);

	/* lookup fields array: 'lookup field name' => array('parent table name', 'lookup field caption') */
	$lookup_fields = array(  'respondent_id' => array('respondents', 'Name'), 'question_id' => array('session_questions', 'Question id'));
	foreach($lookup_fields as $luf => $ptfc){
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if($pt_perm['view'] || $pt_perm['edit']){
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent hspacer-md" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] && !$_REQUEST['Embedded']){
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-success add_new_parent hspacer-md" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus-sign"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(respondent_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(question_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(response)%%>', '', $templateCode);

	// process values
	if($selected_id){
		if( $dvprint) $templateCode = str_replace('<%%VALUE(id)%%>', safe_html($urow['id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(id)%%>', html_attr($row['id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode($urow['id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(respondent_id)%%>', safe_html($urow['respondent_id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(respondent_id)%%>', html_attr($row['respondent_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(respondent_id)%%>', urlencode($urow['respondent_id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(question_id)%%>', safe_html($urow['question_id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(question_id)%%>', html_attr($row['question_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(question_id)%%>', urlencode($urow['question_id']), $templateCode);
		if($dvprint || (!$AllowUpdate && !$AllowInsert)){
			$templateCode = str_replace('<%%VALUE(response)%%>', safe_html($urow['response']), $templateCode);
		}else{
			$templateCode = str_replace('<%%VALUE(response)%%>', html_attr($row['response']), $templateCode);
		}
		$templateCode = str_replace('<%%URLVALUE(response)%%>', urlencode($urow['response']), $templateCode);
	}else{
		$templateCode = str_replace('<%%VALUE(id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(respondent_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(respondent_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(question_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(question_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(response)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(response)%%>', urlencode(''), $templateCode);
	}

	// process translations
	foreach($Translation as $symbol=>$trans){
		$templateCode = str_replace("<%%TRANSLATION($symbol)%%>", $trans, $templateCode);
	}

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if($_REQUEST['dvprint_x'] == ''){
		$templateCode .= "\n\n<script>\$j(function(){\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption){
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$selected_id){
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('responses');
	if($selected_id){
		$jdata = get_joined_record('responses', $selected_id);
		if($jdata === false) $jdata = get_defaults('responses');
		$rdata = $row;
	}
	$templateCode .= loadView('responses-ajax-cache', array('rdata' => $rdata, 'jdata' => $jdata));

	// hook: responses_dv
	if(function_exists('responses_dv')){
		$args=array();
		responses_dv(($selected_id ? $selected_id : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}
?>