<?php
// This script and data application were generated by AppGini 5.74
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/responses.php");
	include("$currDir/responses_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('responses');
	if(!$perm[0]){
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "responses";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = array(   
		"`responses`.`id`" => "id",
		"IF(    CHAR_LENGTH(`respondents1`.`name`), CONCAT_WS('',   `respondents1`.`name`), '') /* Name */" => "respondent_id",
		"IF(    CHAR_LENGTH(`questions1`.`question`), CONCAT_WS('',   `questions1`.`question`), '') /* Question id */" => "question_id",
		"`responses`.`response`" => "response"
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(   
		1 => '`responses`.`id`',
		2 => '`respondents1`.`name`',
		3 => 3,
		4 => 4
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = array(   
		"`responses`.`id`" => "id",
		"IF(    CHAR_LENGTH(`respondents1`.`name`), CONCAT_WS('',   `respondents1`.`name`), '') /* Name */" => "respondent_id",
		"IF(    CHAR_LENGTH(`questions1`.`question`), CONCAT_WS('',   `questions1`.`question`), '') /* Question id */" => "question_id",
		"`responses`.`response`" => "response"
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters = array(   
		"`responses`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`respondents1`.`name`), CONCAT_WS('',   `respondents1`.`name`), '') /* Name */" => "Name",
		"IF(    CHAR_LENGTH(`questions1`.`question`), CONCAT_WS('',   `questions1`.`question`), '') /* Question id */" => "Question id",
		"`responses`.`response`" => "Response"
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS = array(   
		"`responses`.`id`" => "id",
		"IF(    CHAR_LENGTH(`respondents1`.`name`), CONCAT_WS('',   `respondents1`.`name`), '') /* Name */" => "respondent_id",
		"IF(    CHAR_LENGTH(`questions1`.`question`), CONCAT_WS('',   `questions1`.`question`), '') /* Question id */" => "question_id",
		"`responses`.`response`" => "response"
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array(  'respondent_id' => 'Name', 'question_id' => 'Question id');

	$x->QueryFrom = "`responses` LEFT JOIN `respondents` as respondents1 ON `respondents1`.`id`=`responses`.`respondent_id` LEFT JOIN `session_questions` as session_questions1 ON `session_questions1`.`id`=`responses`.`question_id` LEFT JOIN `questions` as questions1 ON `questions1`.`id`=`session_questions1`.`question_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = false;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 0;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "responses_view.php";
	$x->RedirectAfterInsert = "responses_view.php?SelectedID=#ID#";
	$x->TableTitle = "Responses";
	$x->TableIcon = "table.gif";
	$x->PrimaryKey = "`responses`.`id`";

	$x->ColWidth   = array(  150, 150, 150);
	$x->ColCaption = array("Name", "Question id", "Response");
	$x->ColFieldName = array('respondent_id', 'question_id', 'response');
	$x->ColNumber  = array(2, 3, 4);

	// template paths below are based on the app main directory
	$x->Template = 'templates/responses_templateTV.html';
	$x->SelectedTemplate = 'templates/responses_templateTVS.html';
	$x->TemplateDV = 'templates/responses_templateDV.html';
	$x->TemplateDVP = 'templates/responses_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HighlightColor = '#FFF0C2';

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))){ $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])){ // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `responses`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='responses' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])){ // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `responses`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='responses' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3){ // view all
		// no further action
	}elseif($perm[2]==0){ // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`responses`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: responses_init
	$render=TRUE;
	if(function_exists('responses_init')){
		$args=array();
		$render=responses_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: responses_header
	$headerCode='';
	if(function_exists('responses_header')){
		$args=array();
		$headerCode=responses_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode){
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: responses_footer
	$footerCode='';
	if(function_exists('responses_footer')){
		$args=array();
		$footerCode=responses_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode){
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>